import { NextResponse } from 'next/server';
import { prisma } from '@/lib/db/prisma';
import { requireUser } from '@/lib/auth/session';
import { z } from 'zod';
const schema=z.object({type:z.enum(['INCOME','EXPENSE']),amount:z.coerce.number().positive(),categoryId:z.string().optional().nullable(),description:z.string().max(200).optional().nullable(),date:z.string(),paymentMethod:z.string().max(80).optional().nullable()});
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser();const {id}=await params;const existing=await prisma.transaction.findFirst({where:{id,userId:user.id}});if(!existing)return NextResponse.json({error:'Not found'},{status:404});const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Invalid data'},{status:400});const d=p.data;if(d.categoryId){const category=await prisma.financeCategory.findFirst({where:{id:d.categoryId,OR:[{userId:user.id},{userId:null}]}});if(!category||category.type!==d.type)return NextResponse.json({error:'Invalid category'},{status:400});}return NextResponse.json(await prisma.transaction.update({where:{id},data:{type:d.type,amount:d.amount,categoryId:d.categoryId||null,description:d.description||null,date:new Date(d.date),paymentMethod:d.paymentMethod||null},include:{category:true}}));}catch{return NextResponse.json({error:'Unable to update'},{status:400});}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser();const {id}=await params;const r=await prisma.transaction.deleteMany({where:{id,userId:user.id}});if(!r.count)return NextResponse.json({error:'Not found'},{status:404});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'Unable to delete'},{status:400});}}
